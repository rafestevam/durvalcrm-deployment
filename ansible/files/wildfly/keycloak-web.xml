<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee 
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>DurvalCRM Application with Keycloak Security</display-name>
    
    <!-- Keycloak Configuration -->
    <context-param>
        <param-name>keycloak.config.resolver</param-name>
        <param-value>org.keycloak.adapters.KeycloakConfigResolver</param-value>
    </context-param>

    <!-- Security Filter for Keycloak -->
    <filter>
        <filter-name>Keycloak Filter</filter-name>
        <filter-class>org.keycloak.adapters.servlet.KeycloakOIDCFilter</filter-class>
    </filter>
    
    <filter-mapping>
        <filter-name>Keycloak Filter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>

    <!-- Security Constraints -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Protected API Resources</web-resource-name>
            <url-pattern>/api/associados/*</url-pattern>
            <url-pattern>/api/mensalidades/*</url-pattern>
            <url-pattern>/api/doacoes/*</url-pattern>
            <url-pattern>/api/vendas/*</url-pattern>
            <url-pattern>/api/dashboard/*</url-pattern>
            <http-method>GET</http-method>
            <http-method>POST</http-method>
            <http-method>PUT</http-method>
            <http-method>DELETE</http-method>
        </web-resource-collection>
        <auth-constraint>
            <role-name>user</role-name>
            <role-name>admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Public resources (no authentication required) -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Public Resources</web-resource-name>
            <url-pattern>/api/health</url-pattern>
            <url-pattern>/auth/*</url-pattern>
            <url-pattern>/css/*</url-pattern>
            <url-pattern>/js/*</url-pattern>
            <url-pattern>/images/*</url-pattern>
            <http-method>GET</http-method>
        </web-resource-collection>
        <!-- No auth-constraint means public access -->
    </security-constraint>

    <!-- Administrative resources (admin role required) -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Admin Resources</web-resource-name>
            <url-pattern>/api/admin/*</url-pattern>
            <http-method>GET</http-method>
            <http-method>POST</http-method>
            <http-method>PUT</http-method>
            <http-method>DELETE</http-method>
        </web-resource-collection>
        <auth-constraint>
            <role-name>admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Login Configuration -->
    <login-config>
        <auth-method>KEYCLOAK</auth-method>
        <realm-name>durval-crm</realm-name>
    </login-config>

    <!-- Security Roles -->
    <security-role>
        <description>Standard user role for authenticated users</description>
        <role-name>user</role-name>
    </security-role>
    
    <security-role>
        <description>Administrative role for system administrators</description>
        <role-name>admin</role-name>
    </security-role>

    <!-- CORS Configuration -->
    <filter>
        <filter-name>CORS</filter-name>
        <filter-class>org.keycloak.adapters.cors.CorsFilter</filter-class>
        <init-param>
            <param-name>cors.allowed.origins</param-name>
            <param-value>https://20.127.155.169:8443,https://20.127.155.169:9443</param-value>
        </init-param>
        <init-param>
            <param-name>cors.allowed.methods</param-name>
            <param-value>GET,POST,PUT,DELETE,OPTIONS</param-value>
        </init-param>
        <init-param>
            <param-name>cors.allowed.headers</param-name>
            <param-value>origin,accept,x-requested-with,content-type,access-control-request-method,access-control-request-headers,authorization</param-value>
        </init-param>
        <init-param>
            <param-name>cors.exposed.headers</param-name>
            <param-value>access-control-allow-origin,access-control-allow-credentials</param-value>
        </init-param>
        <init-param>
            <param-name>cors.support.credentials</param-name>
            <param-value>true</param-value>
        </init-param>
        <init-param>
            <param-name>cors.preflight.maxage</param-name>
            <param-value>1</param-value>
        </init-param>
    </filter>
    
    <filter-mapping>
        <filter-name>CORS</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Session Configuration -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <secure>true</secure>
            <http-only>true</http-only>
        </cookie-config>
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

</web-app>